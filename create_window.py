# TK means tool kit and inter for interface
# the tk toolkit provides a set of widgets (buttons,text boxes, labels etc) that
#  you can use to build the visual parts of application. that helps you create 
# graphical user interfaces for your application.
import tkinter
root=tkinter.Tk()
#.Tk means tool kit (remember,button,tex boxex,lables etc) root is often used to represente the main window of the  application
# root is a veriable got permission to place all the tools from the tkinter library. root= will now becomes your main window or container
root.mainloop()
#mainloop() keeps the apps window open and active by keeping it in loop

